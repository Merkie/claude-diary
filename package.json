{
	"name": "claude-diary",
	"version": "0.1.0",
	"private": true,
	"description": "Turn your Claude Code conversations into a daily dev diary",
	"type": "module",
	"scripts": {
		"extract": "tsx scripts/extract.ts",
		"dev": "npm run dev --prefix app",
		"build": "npm run build --prefix app"
	},
	"devDependencies": {
		"tsx": "^4.19.4",
		"typescript": "^5.8.3"
	}
}
